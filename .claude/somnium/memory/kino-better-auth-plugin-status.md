---
created_at: 2026-06-02T07:43:23.936787+00:00
updated_at: 2026-06-02T08:33:49.125741+00:00
category: project_memory
source: dream
---

# kino better-auth plugin status

kino better-auth plugin status

Better-auth instance lives in `packages/contract/src/auth/auth.ts` (separated from schema tables — see [[kino contract package auth refactor]]).

**Active plugins (server):** `emailAndPassword({ autoSignIn: true })`, `nextCookies()`, `admin({ defaultRole: "user" })`

**Organization plugin: NOT active.** Tables `organization`, `member`, `invitation` and `session.activeOrganizationId` do NOT exist in the schema. `orgProcedure`/`orgMiddleware` are wired in server/ but non-functional.

**Active plugins (client):** `adminClient()` in `apps/web/src/lib/auth-client.ts`. `organizationClient()` is NOT present (server plugin is off).

**Schema structure** (`packages/contract/src/schema/auth/`) — pm-os per-feature layout:
- `schema.ts` — tables generated by `bun auth:generate` (pgTable definitions; strip the `relations()` blocks the CLI emits — they go in `relations.ts` instead)
- `relations.ts` — drizzle v0 `relations()` style (manual, NOT touched by CLI)
- `types.ts` — `$inferSelect` types (User, Session, Account, Verification)
- `index.ts` — re-exports all three

**Drizzle versions:** `drizzle-orm ^0.45.2` + `drizzle-kit ^0.31.10` (v0 stable) in both `packages/contract` and `apps/web`. Matches pm-os — v0 is required because better-auth's CLI generates `relations()` which is only available in v0.

**DB state:** Migration `0000_init_auth` applied. Tables in Postgres: `user`, `session`, `account`, `verification`.
